// primitive data types
let a = 10
let b = 'janisar hyder'
let c = false

// non-primitive data types (Arrays, tuples, enums)

let x = [1,2,3,4,5,6]

let y : [string,number]= ['janisar', 1234]

enum UserRoles {
    ADMIN = 'admin',
    GUEST = 'guest',
    SUPER_ADMIN = 'super_admin'
}
console.log(UserRoles.SUPER_ADMIN);

